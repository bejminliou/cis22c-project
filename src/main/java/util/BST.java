package main.java.util;

// BST Implementation (unknown if own or from a library)
// Make sure the BST can handle duplicate keys for the user search (ArrayList return)

public class BST<T extends Comparable<T>> {
    // ... (BST implementation - insert, delete, search, etc.) 
    // copy from labs
}